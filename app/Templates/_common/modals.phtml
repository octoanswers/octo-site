
<?php if (isset($this->includeModals) && count($this->includeModals)): ?>
    <!-- modal windows -->
    <?php foreach ($this->includeModals as $modalFilename): ?>
        <?php require_once TEMPLATE_PATH.'/_modal/'.$modalFilename.'.phtml' ?>
    <?php endforeach; ?>
    <!-- / modal windows -->
<?php endif; ?>

