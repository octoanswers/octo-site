<!-- category header -->
<div class="mb-4">
    <p class="mb-0 text-muted mb-2">
        <?= $this->translator->get('category', 'title') ?>
    </p>
    <h1><?= $this->category->title ?></h1>
    <div class="mb-0 text-secondary">
        <?= $this->translator->get('category', 'total_questions') ?>: <?= count($this->category_questions) ?>
    </div>
</div>
<!-- / category header -->