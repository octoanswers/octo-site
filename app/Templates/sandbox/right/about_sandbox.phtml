
<!-- about_sandbox -->
<div class="bg-light p-0 p-md-3 mb-4 d-none d-lg-block">
	<div class="border-bottom pb-2 mb-2 h6 font-weight-bold">
		<?= $this->translator->get('sandbox', 'about_block__title') ?>
	</div>

	<p><?= $this->translator->get('sandbox', 'about_block__msg_1') ?></p>
	<p><?= $this->translator->get('sandbox', 'about_block__msg_2') ?></p>

    <?php if (isset($this->questionsCount)): ?>
        <p class="h1 mb-0"><?= $this->questionsCount ?></p>
        <p class="text-secondary mb-2">
            <?= $this->translator->get('sandbox', 'about_block__now_in_the_sandbox') ?> 
        </p>
    <?php endif; ?>
    
    <?php if (!$this->authUser): ?>
        <button role="button" data-toggle="modal" data-target="#modal_signup" class="btn btn-danger mt-3">
            <?= $this->translator->get('Join the community') ?>
        </button>
        <div class="text-secondary sall mt-1">
            <?= $this->translator->get('or <a data-toggle="modal" data-target="#modal_login">log-in</a>') ?>
        </div>
    <?php endif; ?>
</div>
<!-- / about_sandbox -->
