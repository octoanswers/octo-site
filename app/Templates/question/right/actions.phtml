
<!-- right_actions -->

<nav class="my-4">
	<div class="h5">
        <?= $this->translator->get('Actions') ?>
	</div>
	<ul class="list-links">
        
        <!-- add or update image -->
		<?php if ($this->authUser): ?>
			<li>
				<a href="#" data-toggle="modal" data-target="#modal_upload_image">
					<i class="fas fa-camera w-24px"></i>
					<?php if ($this->question->imageBaseName): ?>
						<?= $this->translator->get('Update picture') ?>
					<?php else: ?>
						<?= $this->translator->get('Add picture') ?>
					<?php endif; ?>
				</a>
			</li>
		<?php endif; ?>
		<!-- add or update image -->

		<?php if ($this->authUser): ?>
			<!-- rename question -->
			<li>
				<a href="#" data-toggle="modal" data-target="#modal_rename">
					<i class="fas fa-edit w-24px"></i>&nbsp;<?= $this->translator->get('Rename question') ?>	
				</a>
			</li>
			<!-- / rename question -->
		<?php endif; ?>
			
		<li>
			<a href="#" rel="nofollow">
                <i class="fas fa-flag w-24px"></i>&nbsp;<s><?= $this->translator->get('Report') ?></s>
			</a>
        </li>
        
        <li>
            <a rel="nofollow" target="_blank" href="<?= DDG_URL_Helper::getSearchURL($this->question->title) ?>">
            <i class="fas fa-search w-24px"></i>&nbsp;<?= $this->translator->get('Search in DuckDuckGo') ?>
            </a>
        </li>

	</ul>
</nav>

<!-- / right_actions -->

