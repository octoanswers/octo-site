
<?php if (count($this->contributors)): ?>
	<!-- contributors_full -->
	<div class="my-5" id="contributors_full_list">
    
        <div class="h4 mb-4">
            <?= $this->translator->get('Answer contributors') ?> 
            <small class="text-muted">
                <span class="ml-3"><?= count($this->contributors) ?></span> <i class="far fa-user"></i>
            </small>
        </div>

		<?php foreach ($this->contributors as $contributor): ?>
			<a class="wh-3 mr-2 mb-2" href="<?= $contributor->getURL($this->lang) ?>">
				<img class="wh-3 rounded-circle mt-1" src="<?= $contributor->getAvatarMediumURL() ?>" alt="<?= $contributor->name ?>">
			</a>
		<?php endforeach; ?>
	</div>
	<!-- / contributors tail -->
<?php endif; ?>
