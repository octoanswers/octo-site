<?php if (count($this->categories)) : ?>

<!-- categories -->
<div class="my-5" id="question_categories_list">
	<div class="text-uppercase mb-3 text-secondary">
		<?= $this->translator->get('question', 'categories_block', 'title') ?>
	</div>

	<?php foreach ($this->categories as $category) : ?>
	<a class="btn btn-sm btn-info mb-2 mr-2 mr-lg-3" href="<?= $category->get_URL($this->lang) ?>">
		<?= $category->title ?>
	</a>
	<?php endforeach; ?>

	<?php if ($this->authUser) : ?>
	<a class="btn btn-sm mb-2" href="<?= $this->question->get_update_categories_URL($this->lang) ?>" rel="nofollow">
		<i class="material-icons md-18">edit</i>&nbsp;<?= $this->translator->get('question', 'categories_block', 'categories_change') ?>
	</a>
	<?php endif; ?>

</div>
<!-- / categories -->

<?php else : ?>

<?php if ($this->authUser) : ?>
<div class="my-5" id="question_categories_list">
	<div class="subtitle-1 mb-3 text-secondary">
		<?= $this->translator->get('question', 'categories_block', 'title') ?>
	</div>

	<a class="btn btn-outline-primary mb-2 mr-2" href="<?= $this->question->get_update_categories_URL($this->lang) ?>">
		<i class="material-icons md-18">add</i>
		<?= $this->translator->get('question', 'categories_block', 'categories_add') ?>
	</a>
</div>
<?php endif; ?>

<?php endif; ?>