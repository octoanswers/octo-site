
<?php if ($this->authUser && !$this->question->getImageBaseName()): ?>
	
	<!-- image_add -->
	<div class="mb-4">
		<a  class="btn btn-sm btn-outline-primary" href="#" data-toggle="modal" data-target="#modal_upload_image">
			<i class="fas fa-camera mr-1"></i>
			<?= _('Add a picture to the question') ?>
		</a>
	</div>
	<!-- / image_add -->

<?php endif; ?>
